package com.example.anfcodetest

import org.junit.Test

import org.junit.Assert.*

// Because the MainPresenter is changing so that the call to fetch the data happens in the Presenter,
// testing will be easier.  
class MainPresenterTest {

    @Test
    fun cardSetup() {
    }
}
