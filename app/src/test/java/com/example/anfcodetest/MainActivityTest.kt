package com.example.anfcodetest

import androidx.recyclerview.widget.LinearLayoutManager
import kotlinx.android.synthetic.main.activity_main.*
import org.junit.Test

import org.junit.Assert.*
import org.mockito.Mock
import org.mockito.Mockito.mock

// You can use either Espresso or Roboletric.  I suggest checking out and follow up with some questions.
// 1. Unit Testing -> http://robolectric.org/ (what you want for this)
// 2. UI Testing -> https://developer.android.com/training/testing/espresso (what could go in androidTest)
class MainActivityTest {

    @Test
    fun loadCards() {
    }

    @Test
    fun fetchJson() {
    }
}
